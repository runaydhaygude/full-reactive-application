## Running this application. 

Run following command from root dir to start the application

`docker-compose -f docker/docker-compose.yml up`


Access the application on browser using `http://localhost:4200`


To stop the application (run on new terminal tab)

`docker-compose -f docker/docker-compose.yml down`